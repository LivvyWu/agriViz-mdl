import { DatagridDirective } from './datagrid.directive';

describe('DatagridDirective', () => {
  it('should create an instance', () => {
    const directive = new DatagridDirective();
    expect(directive).toBeTruthy();
  });
});
